Drop your protagonist portrait here as:

    protagonist.png

Recommended:
- Square or near-square (e.g. 1024 x 1024). The dialogue draws it with ScaleToFit, so any aspect works.
- Transparent background looks best (Hades-style cutout).
- In Unity, select the imported file -> Inspector -> Texture Type: "Default" (Read/Write off is fine).

Used by: Assets/Scripts/UI/YamatoArrivalDialogue.cs
Loaded via: Resources.Load<Texture2D>("Characters/protagonist")
